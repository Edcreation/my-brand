version: 2.1
jobs: 
  run_tests:
    docker: 
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: npm install 
      - run:
          name: Run Unit Tests
          command: npm test
workflows:
  build_tests:
    jobs:
      - run_tests